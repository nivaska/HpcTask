<Window x:Class="ShipCrudApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShipCrudApp"
        xmlns:vm="clr-namespace:ShipCrudApp.ViewModels"
        xmlns:views="clr-namespace:ShipCrudApp.Views"
        mc:Ignorable="d"
        Title="Ships Manager" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Grid>
        <TabControl>
            <TabItem x:Name="Tab1" Header="All Ships" >
                <views:AllShipsView></views:AllShipsView>
            </TabItem>
            <TabItem x:Name="Tab2" Header="New Ship" >
                <views:NewShipView></views:NewShipView>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
